# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Environments
.env
/credentials/
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Project specifics
# ML weights / engines (leading slash so it never matches app/models/)
/models/
/ml_m/
# Anchored to root for the same reason as /models/ above — an unanchored
# "output/" would also match any future app/**/output/ directory.
/output/
/output_frame/
/Data_Collection/
/rejected/
*.log

# IDEs
.vscode/
.idea/
